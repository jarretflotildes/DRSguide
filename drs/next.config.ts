import type { NextConfig } from "next";

// If you have this in next.config.js:
module.exports = {
  distDir: 'build', // This would create 'build' folder instead of '.next'
}

const nextConfig: NextConfig = {  
};

export default nextConfig;
